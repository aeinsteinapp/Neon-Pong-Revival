{
  "name": "neon-pong-revival",
  "version": "1.0.0",
  "description": "HTML5 Pong wrapped for mobile.",
  "scripts": {
    "start": "npx http-server ./www",
    "cap:add:android": "node_modules/.bin/cap add android",
    "cap:copy": "node_modules/.bin/cap copy",
    "cap:sync": "node_modules/.bin/cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
